<Window x:Class="IgniteEditorV2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ignite Editor V2" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        Foreground="#FFFFFF">
    
    <Window.Resources>
        <!-- Dark Theme Colors -->
        <SolidColorBrush x:Key="DarkBackgroundBrush" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="DarkSurfaceBrush" Color="#2D2D30"/>
        <SolidColorBrush x:Key="DarkAccentBrush" Color="#007ACC"/>
        <SolidColorBrush x:Key="DarkBorderBrush" Color="#3F3F46"/>
        <SolidColorBrush x:Key="DarkTextBrush" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="DarkTextSecondaryBrush" Color="#969696"/>
        <SolidColorBrush x:Key="DarkHoverBrush" Color="#383838"/>
        <SolidColorBrush x:Key="DarkSelectedBrush" Color="#094771"/>
        
        <!-- Professional Button Style -->
        <Style x:Key="DefaultButton" TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="ButtonBorder" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#383838"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#094771"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#1F8AD3"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#252526"/>
                                <Setter Property="Foreground" Value="#656565"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#2D2D30"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Dark Menu Style -->
        <Style TargetType="Menu">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
        
        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border Name="Border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                    <ColumnDefinition Width="13"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" ContentSource="Icon" Margin="6,0,6,0" VerticalAlignment="Center"/>
                                <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True" Margin="0,3"/>
                                <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}" Margin="5,3,0,3"/>
                                <Path Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 0 7 L 4 3.5 Z" Fill="#CCCCCC" Name="ArrowPath" Visibility="Collapsed"/>
                                <Popup Name="Popup" Placement="Right" IsOpen="{TemplateBinding IsSubmenuOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                    <Border Name="SubmenuBorder" SnapsToDevicePixels="True" Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="1">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Role" Value="TopLevelHeader">
                                <Setter Property="Grid.IsSharedSizeScope" Value="true"/>
                                <Setter Property="Placement" Value="Bottom" TargetName="Popup"/>
                            </Trigger>
                            <Trigger Property="Role" Value="SubmenuHeader">
                                <Setter Property="Visibility" Value="Visible" TargetName="ArrowPath"/>
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter Property="Background" Value="#383838" TargetName="Border"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#656565"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Dark TreeView Style -->
        <Style TargetType="TreeView">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
        </Style>
        
        <Style TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition MinWidth="19" Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <ToggleButton x:Name="Expander" Grid.Column="0" Grid.Row="0" 
                                        ClickMode="Press" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                        Background="Transparent" BorderThickness="0">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="Transparent" Width="16" Height="16">
                                                        <Path Name="ExpandPath" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                              Margin="1,1,1,1" Fill="#CCCCCC" Data="M 4 0 L 8 4 L 4 8 Z"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter Property="Data" TargetName="ExpandPath" Value="M 0 4 L 8 4 L 4 8 Z"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                            <Border Name="Bd" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="0" 
                                   Background="{TemplateBinding Background}" 
                                   BorderBrush="{TemplateBinding BorderBrush}" 
                                   BorderThickness="{TemplateBinding BorderThickness}" 
                                   Padding="{TemplateBinding Padding}">
                                <ContentPresenter x:Name="PART_Header" ContentSource="Header" 
                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="false">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="#094771"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="IsSelectionActive" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="#3F3F46"/>
                            </MultiTrigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="#383838"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#656565"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Dark TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Name="Border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#1F8AD3"/>
                                <Setter TargetName="Border" Property="Background" Value="#252526"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Dark StatusBar Style -->
        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="0,1,0,0"/>
        </Style>
        
        <!-- Dark Border Style -->
        <Style x:Key="DarkPanel" TargetType="Border">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <!-- Dark Panel Header Style -->
        <Style x:Key="PanelHeader" TargetType="TextBlock">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <!-- Custom GridSplitter Style -->
        <Style TargetType="GridSplitter">
            <Setter Property="Background" Value="#3F3F46"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridSplitter">
                        <Border Background="{TemplateBinding Background}">
                            <Rectangle Fill="#3F3F46" Width="1" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Menu Bar -->
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_New" Click="NewFile_Click"/>
                <MenuItem Header="_Open" Click="OpenFile_Click"/>
                <MenuItem Header="_Save" Click="SaveFile_Click"/>
                <Separator/>
                <MenuItem Header="E_xit" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo"/>
                <MenuItem Header="_Redo"/>
                <Separator/>
                <MenuItem Header="Cu_t"/>
                <MenuItem Header="_Copy"/>
                <MenuItem Header="_Paste"/>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="_Hierarchy"/>
                <MenuItem Header="_Inspector"/>
                <MenuItem Header="_Console"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About" Click="About_Click"/>
            </MenuItem>
        </Menu>
        
        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" MinWidth="150"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="250" MinWidth="200"/>
            </Grid.ColumnDefinitions>
            
            <!-- Hierarchy Panel -->
            <Border Grid.Column="0" Style="{StaticResource DarkPanel}" Margin="5">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Hierarchy" Style="{StaticResource PanelHeader}"/>
                    <TreeView Name="HierarchyTreeView">
                        <TreeViewItem Header="Scene" IsExpanded="True">
                            <TreeViewItem Header="🎥 Main Camera"/>
                            <TreeViewItem Header="💡 Directional Light"/>
                            <TreeViewItem Header="📦 GameObject" IsExpanded="True">
                                <TreeViewItem Header="🧊 Cube"/>
                                <TreeViewItem Header="⚪ Sphere"/>
                                <TreeViewItem Header="🌍 Terrain"/>
                            </TreeViewItem>
                        </TreeViewItem>
                    </TreeView>
                </DockPanel>
            </Border>
            
            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" 
                         ResizeBehavior="PreviousAndNext"/>
            
            <!-- Scene/Game View -->
            <Border Grid.Column="2" Style="{StaticResource DarkPanel}" Margin="5">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" 
                               Background="#2D2D30" Height="35">
                        <Button Content="Scene" Style="{StaticResource DefaultButton}" 
                                Click="SceneView_Click" Margin="5,4"/>
                        <Button Content="Game" Style="{StaticResource DefaultButton}" 
                                Click="GameView_Click" Margin="2,4"/>
                        <Separator Background="#3F3F46" Margin="8,6" Width="1"/>
                        <Button Content="▶️ Play" Style="{StaticResource DefaultButton}" 
                                Margin="2,4" Foreground="#4CAF50" Click="Play_Click"/>
                        <Button Content="⏸️ Pause" Style="{StaticResource DefaultButton}" 
                                Margin="2,4" Foreground="#FF9800" Click="Pause_Click"/>
                        <Button Content="⏹️ Stop" Style="{StaticResource DefaultButton}" 
                                Margin="2,4" Foreground="#F44336" Click="Stop_Click"/>
                    </StackPanel>
                    <Border Background="#1A1A1A" BorderBrush="#3F3F46" BorderThickness="1">
                        <Grid>
                            <Rectangle Fill="#1A1A1A"/>
                            <TextBlock Text="🎮 Scene/Game View" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"
                                      FontSize="16" 
                                      Foreground="#666666"
                                      FontWeight="Light"/>
                            <!-- Grid overlay for scene view -->
                            <Canvas>
                                <Line X1="0" Y1="50" X2="100" Y2="50" Stroke="#333333" StrokeThickness="0.5" Opacity="0.3"/>
                                <Line X1="50" Y1="0" X2="50" Y2="100" Stroke="#333333" StrokeThickness="0.5" Opacity="0.3"/>
                            </Canvas>
                        </Grid>
                    </Border>
                </DockPanel>
            </Border>
            
            <!-- Splitter -->
            <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch" 
                         ResizeBehavior="PreviousAndNext"/>
            
            <!-- Inspector Panel -->
            <Border Grid.Column="4" Style="{StaticResource DarkPanel}" Margin="5">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Inspector" Style="{StaticResource PanelHeader}"/>
                    <ScrollViewer Background="#252526">
                        <StackPanel Margin="12">
                            <!-- Transform Component -->
                            <Border Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="1" 
                                   CornerRadius="3" Margin="0,0,0,8">
                                <StackPanel>
                                    <Border Background="#383838" Padding="8,4">
                                        <TextBlock Text="🔧 Transform" FontWeight="SemiBold" 
                                                  Foreground="#CCCCCC" FontSize="11"/>
                                    </Border>
                                    <Grid Margin="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        
                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Position" 
                                                  Foreground="#CCCCCC" Margin="0,4"/>
                                        <TextBox Grid.Row="0" Grid.Column="1" Text="0, 0, 0" Margin="4"/>
                                        
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Rotation" 
                                                  Foreground="#CCCCCC" Margin="0,4"/>
                                        <TextBox Grid.Row="1" Grid.Column="1" Text="0, 0, 0" Margin="4"/>
                                        
                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Scale" 
                                                  Foreground="#CCCCCC" Margin="0,4"/>
                                        <TextBox Grid.Row="2" Grid.Column="1" Text="1, 1, 1" Margin="4"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                            
                            <!-- Mesh Renderer Component -->
                            <Border Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="1" 
                                   CornerRadius="3" Margin="0,0,0,8">
                                <StackPanel>
                                    <Border Background="#383838" Padding="8,4">
                                        <TextBlock Text="🎨 Mesh Renderer" FontWeight="SemiBold" 
                                                  Foreground="#CCCCCC" FontSize="11"/>
                                    </Border>
                                    <Grid Margin="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        
                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Material" 
                                                  Foreground="#CCCCCC" Margin="0,4"/>
                                        <Button Grid.Row="0" Grid.Column="1" Content="Default Material" 
                                               Style="{StaticResource DefaultButton}" 
                                               HorizontalAlignment="Stretch" Margin="4"/>
                                        
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Mesh" 
                                                  Foreground="#CCCCCC" Margin="0,4"/>
                                        <Button Grid.Row="1" Grid.Column="1" Content="Cube" 
                                               Style="{StaticResource DefaultButton}" 
                                               HorizontalAlignment="Stretch" Margin="4"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                            
                            <!-- Add Component Button -->
                            <Button Content="➕ Add Component" Style="{StaticResource DefaultButton}" 
                                   HorizontalAlignment="Stretch" Margin="0,8,0,0" Padding="0,8"/>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="●" Foreground="#4CAF50" Margin="0,0,4,0"/>
                    <TextBlock Text="Ready" Name="StatusText"/>
                </StackPanel>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⚡ Ignite Editor V2" Margin="0,0,8,0"/>
                    <Rectangle Width="1" Fill="#FFFFFF" Opacity="0.3" Margin="0,2"/>
                    <TextBlock Text="CMake WPF" Margin="8,0,0,0" FontSize="10" Opacity="0.8"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
